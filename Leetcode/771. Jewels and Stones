class Solution:
    def numJewelsInStones(self, J, S):      
         a= len(J)
         print(a)
         b=len(S)
         lst1=list(J)
         lst2= list(S) 
         count=0
         for i in range(0,a):
             for j in range(0,b):
                    if lst2[j] == lst1[i]:
                     count=count+1
         return count
