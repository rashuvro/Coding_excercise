class Solution:
    def fib(self, n):
        a=[]
        a.append(0)
        a.append(1)
        if n==0:
            return a[0]
        elif n==1:
            return a[1]
        else:
            for i in range(2,n+1):
                b=a[i-1]+a[i-2]
                a.append(b)

        return (a[n])
        
